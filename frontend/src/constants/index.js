/**
 * App-wide constants.
 *
 * Centralise magic strings and configuration values here
 * so they can be updated in a single place.
 */

/** Route paths */
export const ROUTES = {
    HOME: '/',
    ABOUT: '/about',
    LOGIN: '/login',
    SIGNUP: '/signup',
};
