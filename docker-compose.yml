# FixMyText — unified compose file with profiles.
#
# Profiles:
#   dev   development stack  (hot reload, source volumes)
#   prod  production stack   (multi-stage builds, nginx)
#
# Usage:
#   docker compose --profile dev up --build     # dev
#   docker compose --profile prod up --build    # prod
#
# Or via npm:
#   npm run docker:dev    # dev stack
#   npm run docker:up     # prod stack

services:

  # ══════════════════════════════════════════════════════════════════════════
  # DEV  —  profile: fixmytext-dev
  # ══════════════════════════════════════════════════════════════════════════

  backend-dev:
    profiles: ["dev"]
    build:
      context: ./backend
      dockerfile: Dockerfile.dev
    container_name: fixmytext-backend-dev
    ports:
      - "8000:8000"
    volumes:
      - ./backend:/app      # mount source — uvicorn --reload picks up changes
      - /app/.venv          # anonymous volume keeps host .venv out of the container
    env_file:
      - ./backend/.env
    environment:
      - HOST=0.0.0.0
      - PORT=8000
      - DEBUG=true
      - ALLOWED_ORIGINS=["http://localhost:3000","http://127.0.0.1:3000"]
    networks:
      - fixmytext-net

  frontend-dev:
    profiles: ["dev"]
    build:
      context: ./frontend
      dockerfile: Dockerfile.dev
    container_name: fixmytext-frontend-dev
    ports:
      - "3000:3000"
    volumes:
      - ./frontend/src:/app/src          # mount src — Vite hot-reloads on change
      - ./frontend/public:/app/public    # mount public assets
      - ./frontend/index.html:/app/index.html
    environment:
      - VITE_API_URL=http://localhost:8000
    depends_on:
      - backend-dev
    networks:
      - fixmytext-net


  # ══════════════════════════════════════════════════════════════════════════
  # PROD  —  profile: fixmytext-prod
  # ══════════════════════════════════════════════════════════════════════════

  backend-prod:
    profiles: ["prod"]
    build:
      context: ./backend
      dockerfile: Dockerfile
      target: runtime
    container_name: fixmytext-backend-prod
    restart: unless-stopped
    ports:
      - "8000:8000"
    env_file:
      - ./backend/.env
    environment:
      - HOST=0.0.0.0
      - PORT=8000
      - DEBUG=false
      - ALLOWED_ORIGINS=["http://localhost:3000","http://frontend-prod:80"]
    networks:
      - fixmytext-net
    healthcheck:
      test: ["CMD", "python", "-c",
             "import urllib.request; urllib.request.urlopen('http://localhost:8000/health')"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 15s

  frontend-prod:
    profiles: ["prod"]
    build:
      context: ./frontend
      dockerfile: Dockerfile
      target: runtime
      args:
        VITE_API_URL: http://localhost:8000
    container_name: fixmytext-frontend-prod
    restart: unless-stopped
    ports:
      - "3000:80"
    depends_on:
      backend-prod:
        condition: service_healthy
    networks:
      - fixmytext-net
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:80"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s


# ── Shared network ────────────────────────────────────────────────────────────
networks:
  fixmytext-net:
    driver: bridge
